class Solution {
    static String delAlternate(String S) {
        String s="";
       for(int i=0;i<S.length();i++)
       {
        if(i%2==0)
         s+=S.charAt(i);
       }
       return s;
    }
}
