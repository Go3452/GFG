class Solution{
	int equalSum(int [] A, int N) {
		int sum=0,sum1=0;
		for(int i=0;i<N;i++)
		 sum+=A[i];
		for(int i=0;i<N;i++)
		{
		 sum-=A[i];
		 if(sum1==sum)
		  return i+1;
		 sum1+=A[i];
		}
		return -1;
	}
}
