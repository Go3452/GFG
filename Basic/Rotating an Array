class Solution {
    void leftRotate(int[] arr, int n, int d) {
        int ar[]=new int[n];
        for(int i=0;i<n;i++)
        {
         ar[i]=arr[(n+d+i)%n];
        }
        for(int i=0;i<n;i++)
         arr[i]=ar[i];
    }
}
