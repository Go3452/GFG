class Solution {
    int totalCount(int[] arr, int n, int k) {
        int count=0;
        for(int i=0;i<n;i++)
        {
         if(arr[i]%k==0)
          count+=arr[i]/k;
         else
          count+=arr[i]/k+1;
        }
        return count;
    }
}
