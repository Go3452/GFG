class Sol
{
    int divisibleBy4 (String N)
    {
     int ans=0;
     if(N.length()<3)
      ans=Integer.parseInt(N);
     else
      ans=Integer.parseInt(N.substring(N.length()-3,N.length()));
     if(ans%4==0) return 1;
     return 0;
    }
}
