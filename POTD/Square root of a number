class Solution {
    long floorSqrt(long n) {
        long i=1,j=n;
        while(i<=j)
        {
         long mid=(i+j)/2;
         if(mid*mid==n)
          return mid;
         else if(mid*mid>n)
          j=mid-1;
         else
          i=mid+1;
        }
        return i-1;
    }
}
